<div class="row">
	<div class="col-xs-6">
		<h3 class="logo"><strong>Create-A-Poll</strong></h3>
	</div>
	<div class="col-xs-6">
	<% if user_signed_in? %>
		<%= link_to("Logout", destroy_user_session_path, class: "btn btn-danger navbar-btn logout", method: :delete) %>
	<% end %>
	</div>
</div>