<div class="row" id="nav-margin">
	<div class="col-xs-6">
		<h3><strong><%= link_to 'Create-A-Poll', polls_path, class: 'logo' %></strong></h3>
	</div>
	
	<div class="col-xs-3">
		<%= link_to 'New Poll', new_poll_path, class: 'btn btn-success btn-add-poll' %>
	</div>

	<div class="col-xs-3">
	<% if user_signed_in? %>
		<%= link_to("Logout", destroy_user_session_path, class: "btn btn-danger logout", method: :delete) %>
	<% end %>
	</div>
</div>